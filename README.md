# RenameAndFolderPhotos

Uses powershell to rename photos based on their taken date and moves them in to a folder structure or Year\Month.

Usage
[PhotoMove]::("Source Directory","DestinationDirectory")
